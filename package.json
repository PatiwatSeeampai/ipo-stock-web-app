{
  "name": "investment-terminal-root",
  "version": "1.0.0",
  "description": "Master launcher for the Professional Investment Terminal (Web UI & Tauri Desktop Wrapper)",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev:backend": "node backend/server.js",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:desktop": "npm run tauri dev --prefix frontend",
    "build:desktop": "npm run tauri build --prefix frontend"
  }
}
